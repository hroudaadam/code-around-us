extends layout.pug

block headScripts   
    script(
        src=htmlWebpackPlugin.options.publicPath + "/" + visualizationId + "/index.js"
    ) 

block content

    div(
        class="d-flex flex-column justify-content-center align-items-center"
    )

        div(
            id="p5-container"
        )

        div
            p=visualizationText

    script(
        src="https://cdn.jsdelivr.net/npm/p5@1.4.1/lib/p5.js"
    )